#include <iostream>
using namespace std;
int main()
{
  string s = "*";
  int a, b;
  cout << "Введите ширину и длину прямоугольника" << endl; 
  cin >> a >> b;
  for (int i = 0; i < a; ++i)
  {
    for (int i = 0; i < b-1; ++i)
    {
      cout << s;
    }
    cout << s << endl;
  }
}
