#include <iostream>
#include <ctime>
using namespace std;
int main()
{
  srand(time(NULL));
  
  int arr[5];
  
  for (int i = 0; i < 5; ++i)
  {
    arr[i] = rand() % 10;
  }

  for (int i = 0; i < 5; ++i)
  {
    cout << "[" << arr[i] << "] ";
  }
  
}
